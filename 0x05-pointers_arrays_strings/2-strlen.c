#include "main.h"

/**
 * _strlen - gets character sting
 *
 * @s: character to get string
 * return: returns length
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{}
	return (i);
}
